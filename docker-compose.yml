services:
  query-builder_composer:
    build:
      dockerfile: Dockerfile
      context: .docker/php
    container_name: query-builder_composer
    restart: "no"
    working_dir: /app
    volumes:
      - ./:/app

volumes:
  db:
networks:
  default:
    external: true
    name: shared-network
